spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xzabalam/microservices-demo_config-server-repository.git
          default-label: master
          clone-on-start: true
    fail-fast: true