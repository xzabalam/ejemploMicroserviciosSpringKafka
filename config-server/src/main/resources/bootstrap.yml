spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:////E:/desarrollo/workspaces/ws-java/microservices-demo/config-server
          clone-on-start: true
    fail-fast: true